name: super_ia
description: SUPER_IA - Plataforma de Gestión Comercial y Crediticia

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.6
  provider: ^6.1.2
  geolocator: ^10.1.0
  pinput: ^5.0.0
  shared_preferences: ^2.3.3
  flutter_map: ^6.1.0
  latlong2: ^0.9.1
  sliding_up_panel: ^2.0.0+1
  carousel_slider: ^5.0.0
  connectivity_plus: ^5.0.2
  flutter_spinkit: ^5.2.1
  http: ^1.2.2
  image_picker: ^1.1.2
  url_launcher: ^6.3.1
  firebase_core: ^2.32.0
  firebase_auth: ^4.20.0
  firebase_messaging: ^14.9.4
  google_fonts: ^6.2.1
  cloud_firestore: ^4.17.5
  intl: ^0.19.0
  audioplayers: ^6.1.0
  flutter_local_notifications: ^17.2.2
  flutter_foreground_task: ^6.1.1
  webview_flutter: ^4.8.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0


flutter:

  uses-material-design: true

  assets:
    - images/
    - assets/
    - assets/sounds/
